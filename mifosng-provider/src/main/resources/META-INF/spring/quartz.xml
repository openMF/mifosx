<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

    <bean id="cronQuartzJob" class="org.mifosplatform.scheduledjobs.Job">
        <constructor-arg type="java.lang.String" value="I am a cron job" />
    </bean>

    <bean id="scheduledQuartzJob" class="org.mifosplatform.scheduledjobs.Job">
        <constructor-arg type="java.lang.String" value="I am a scheduled job" />
    </bean>

    <bean id="cronDetail" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="group" value="myJobs" />
        <property name="targetObject" ref="cronQuartzJob" />
        <property name="targetMethod" value="execute" />
        <property name="concurrent" value="false" />
    </bean>

    <bean id="scheduledDetail" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
        <property name="group" value="myJobs" />
        <property name="targetObject" ref="scheduledQuartzJob" />
        <property name="targetMethod" value="execute" />
        <property name="concurrent" value="false" />
    </bean>

    <!-- create the Trigger with a Schedule that runs every 5 seconds -->
    <bean id="cronTrigger" class="org.springframework.scheduling.quartz.CronTriggerFactoryBean">

        <property name="jobDetail" ref="cronDetail" />
        <property name="cronExpression" value="0 0/1 * 1/1 * ? *" />
    </bean>

    <!--  create a Scheduled Trigger -->
    <bean id="simpleTrigger" class="org.springframework.scheduling.quartz.SimpleTriggerFactoryBean">
        <property name="group" value="myJobs" />
        <property name="jobDetail" ref="scheduledDetail" />
        <!-- 10 seconds -->
        <property name="startDelay" value="10000" />
        <!-- repeat every 5 seconds -->
        <property name="repeatInterval" value="60000" />
    </bean>

    <!-- schedule the Jobs -->
    <bean name="schedulerFactory" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">

        <property name="triggers">
            <list>
                <ref bean="cronTrigger" />
                <!--<ref bean="simpleTrigger" />-->
            </list>
        </property>

    </bean>

</beans>